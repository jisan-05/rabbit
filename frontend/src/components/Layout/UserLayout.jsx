import React from "react";
import Header from "../Common/Header";

const UserLayout = () => {
    return (
        <div>
            <Header></Header>
        </div>
    );
};

export default UserLayout;
